services:
    docent:
        image: ghcr.io/danmunz/docent:latest
        container_name: docent
        network_mode: host
        restart: unless-stopped
        env_file:
            - .env
        volumes:
            - data:/data

volumes:
    data:
