# Run with docker compose up -d
services:
    filegator:
        image: filegator/filegator
        container_name: filegator
        restart: unless-stopped
        networks:
            - caddy

networks:
    caddy:
        external: true
