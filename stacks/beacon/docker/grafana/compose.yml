services:
    grafana:
        image: grafana/grafana-enterprise
        container_name: grafana
        restart: unless-stopped
        ports:
            - 4213:3000
        volumes:
            - storage:/var/lib/grafana
        env_file:
            - .env

volumes:
    storage:
